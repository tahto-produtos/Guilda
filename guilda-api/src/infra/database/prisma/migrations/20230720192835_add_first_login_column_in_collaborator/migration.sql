BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[GDA_COLLABORATORS] ADD [FIRST_LOGIN] BIT NOT NULL CONSTRAINT [GDA_COLLABORATORS_FIRST_LOGIN_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
