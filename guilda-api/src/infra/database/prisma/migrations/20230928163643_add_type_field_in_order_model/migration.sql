BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[GDA_STOCK] ADD [type] NVARCHAR(1000) CONSTRAINT [GDA_STOCK_type_df] DEFAULT 'PHYSICAL';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
